# 设计约定 · 当前个人实践图册

当前功能实现：`3e0383f`（2026-09-21）+ 2026-09-22 生活态度标语与玄枢/显化页（见 Git 历史）。本文件描述当前设计；发布与验证状态以 [README.md](README.md) 为准。

## 定位与视觉

长期个人品牌，兼顾求职。先让访客理解关注的问题、公开工具、代表实践与本人贡献；经历和求职信息保持简洁，不主导首页。

纸色、森林绿、克制的强调色，大字、留白和编辑式章节。主视觉为 `assets/yi-chengxi-illustrated.jpg`：本人照片参考生成的二维插画，原始照片不公开。项目图解解释来源核验、资料归档、协作验收与多体系对拍；概念图不充当业务截图，不将生成素材称为本人手绘或 Blender 建模。

首页主标语为生活态度（2026-09-22 起）：「睡够，留点饿，时常动一动。其他往后排。」——对应本人作息与运动优先级；英文版三行 "Sleep well," / "stay hungry, move" / "the rest can wait."。标语强调行（h1 em）使用强调色 `--rust`（赤陶），其余标题强调保持森林绿；这是全站唯一使用 `--rust` 的大字级元素。该配色（纸色底、森林绿主字、赤陶强调）与本人八字喜用（火、土）方向一致，属个人偏好表达，不作为效果宣称。h1 字号受标语行长硬约束：hero 左栏在桌面恒定约 584px，中文七字行要求上限 ≤5.3rem，英文长行要求上限 4.5rem、手机下限 2.5rem；改标语文字后必须实测三行不折行。vision 宣言为九字单行，clamp 下限 2rem 以在 360px 宽手机一行放下。

## 信息与交互

- 首页提供双语内容；任务工具和同舟案例为中文，英文入口说明语言边界，简历仍为中文。
- 案例阅读器按“问题、我的工作、结果与边界”组织，支持项目选择、步骤深链接与分享；无脚本时保留原生项目详情。
- 工具入口分别解决证据整理和任务交接。同舟案例解释已完成的本地实践，协作计划设计器展示步骤并导出文件，不显示虚假的运行状态。
- 玄枢案例（`projects/xuanshu/`，2026-09-22 新增）呈现多体系命理交叉验证学习项目：三层纪律、15 体系可靠性分档、典籍出处与验证记录如实标注，明示"不作预测服务、不构成建议"。首页以第 4 张项目卡（04 / FIELDWORK）接入。
- 显化仪式页（`vision/`，2026-09-22 新增）为个人每日练习页：一句宣言加三步日课，`noindex`、不在站内导航、不进 sitemap；宣言表述为自我暗示练习而非事实或预言，页面自带边界声明。首页与页脚均不放入口。
- 首页、两款工具和案例共用 `site-theme` 主题偏好。表单错误要能定位；内容变化后旧产物失效；复制失败时给出可选择的原文。
- 以真实产物、明确检查和日期表达成果；不宣称未经验证的用户效果、节省比例或独立工程能力。

## 技术与公开边界

保留静态 HTML/CSS/JavaScript、浏览器原生控件和本地字体，无构建链或客户端框架。字体 woff2 优先、TTF 回退；主视觉经 `<picture>` 按宽度提供 webp，JPG 回退并兼作分享图。工具核心用纯函数，存储与下载留在页面层；各页（含首页）均有 CSP 禁止脚本外连，内联主题初始化脚本以 SHA-256 哈希放行，修改该脚本须重算哈希并同步各页 CSP。主题默认跟随系统偏好，手动选择经 localStorage 跨页记忆，`theme-color` 随主题更新。私人执行器、会员凭证、任务队列与控制接口留在本地，不进入网站。

任务信息草稿用于恢复编辑；工作流 JSON 用于审阅后交接。两种格式须明确区分。代码工具任务只提供单步设计，文字任务支持三阶段；自动阶段标记不代表事实或质量认证。

验收看最终画面和实际行为：桌面/手机、深浅色、键盘焦点、错误与取消路径、下载与导入、长文本和页面溢出。尊重减少动画偏好；工具依赖 JavaScript，首页主要内容可无脚本阅读。测试通过不代替视觉验收，也不代替发布后的线上操作复测。

## 历史设计（已被当前实现替代）

2026-09-20 晚的研究档案改版（方向 B，`4db3a71`）上线后经用户验收不满意，同日全量回滚（`e225f45`）；四方向样稿、审批与验证记录保留在本地 `_dev/design-evolution-20260920/` 与 Git 历史，不作为当前设计。视觉重做按用户决定暂停。

2026-09-22 的标语替换（生活态度）与强调色微调是用户基于个人命理偏好明确授权的小步改动，仅限 h1 em 一处用色；同日新增玄枢案例页与 vision 仪式页。这不构成恢复全面视觉重做——版式、插画与整体配色结构均未动。

2026-09-19 曾评估静物主视觉、CSS 3D 三层工作台、视角调整和模拟输入。当前页面已移除这些交互；旧素材、原型和验证记录保留在原位置及 Git 历史，不作为功能清单。

设计曾参考 [Brittany Chiang](https://brittanychiang.com/) 的内容层次和 [Astro Bento Portfolio](https://github.com/Ladvace/astro-bento-portfolio) 的交互组织，没有导入模板代码。未使用 CAD、Blender 或 Three.js；没有为原型增加 WebGL 或 Next.js 构建依赖。
